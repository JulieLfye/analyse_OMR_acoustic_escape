% save angle_to_OMR

% save(fullfile(a,'angle_to_OMR.mat'),angle_to_OMR)

% clc
%
% F = Focus();
%
% F.dpf = 5;
% F.OMR = 1000;
% F.V = '1_0';
%
% Data = F.load('data.mat');

%% --------------------------------------------------

n = 0;
nb_detected_object = 0;
% for i = 1:nb_frame
    f = 1;
    nb = 0;
    
    while f <= nb_tra_object
        if isnan(s(i*nb_tracked_object + f,1)) == 0
            nb = nb + 1;
            f = f + 1;
        else
            f = f + 1;
        end
    end
    if nb > nb_detected_object
                nb_detected_object = nb;
    end
% end
